import { TipoDeConsultaExame } from './tipo-de-consulta-exame';

describe('TipoDeConsultaExame', () => {
  it('should create an instance', () => {
    expect(new TipoDeConsultaExame()).toBeTruthy();
  });
});
